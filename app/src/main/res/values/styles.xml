<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="colorControlNormal">@color/colorLightGrey</item>
        <item name="android:textColorHint">@color/colorLightGrey</item>
    </style>

    <!--Main Toolbar Theme-->
    <style name="MainToolbar" parent="Theme.AppCompat">
        <item name="android:textColorPrimary">@color/colorGrey</item>
    </style>

    <!--Search Bar Theme-->
    <style name="searchBar" parent="Theme.AppCompat">
        <item name="android:textColorSecondary">@color/colorLightGrey</item>
        <item name="android:editTextColor">@color/colorOffBlack</item>
        <item name="android:searchIcon">@drawable/ic_search</item>
    </style>

    <!--Show Image Theme-->
    <style name="Transparent" parent="@style/ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="android:windowActionBarOverlay">true</item>
        <item name="windowActionBarOverlay">true</item>
        <item name="android:homeAsUpIndicator">@drawable/ic_transparent_back</item>
        <item name="android:actionOverflowButtonStyle">@style/MenuButton</item>
    </style>

    <style name="MenuButton" parent="android:style/Widget.Holo.Light.ActionButton.Overflow">
        <item name="android:src">@drawable/ic_transparent_menu</item>
        <item name="android:background">?android:attr/actionBarItemBackground</item>
        <item name="android:contentDescription">"Menu Button"</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
